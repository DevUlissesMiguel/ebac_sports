// src/types.ts
// (Estou adivinhando a estrutura do seu tipo, ajuste se necessário)
export interface Produto {
  id: number
  nome: string
  preco: number
  imagem: string
  // ...outras propriedades que ele possa ter
}
